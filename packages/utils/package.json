{
  "name": "@depaneuria/utils",
  "version": "0.1.0",
  "description": "Utilitaires partagés — depaneurIA",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo '[utils] lint: not yet wired' && exit 0",
    "format": "echo '[utils] format: not yet wired' && exit 0"
  },
  "dependencies": {
    "@depaneuria/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
